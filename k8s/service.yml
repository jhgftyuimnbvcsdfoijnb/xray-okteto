apiVersion: v1
kind: Service
metadata:
  name: actiond
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "actiond"
    port: 8080
  selector:
    app: actiond
