apiVersion: v1
kind: Service
metadata:
  name: jactrexv
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jactrexv"
    port: 8080
  selector:
    app: jactrexv
