apiVersion: v1
kind: Service
metadata:
  name: cherim
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cherim"
    port: 8080
  selector:
    app: cherim
