apiVersion: v1
kind: Service
metadata:
  name: shaire
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "shaire"
    port: 8080
  selector:
    app: shaire
